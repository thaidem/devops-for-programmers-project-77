vault_db_host: "${db_host}"
vault_db_name: "${db_name}"
vault_db_port: "${db_port}"
vault_db_user: "${db_user}"
vault_db_password: "${db_password}"

vault_datadog_api_key: "${datadog_api_key}"
vault_datadog_site: "${datadog_site}"
